spring:
  profiles:
    active: dev

---

spring:
  profiles: dev
  application:
    name: gateway-server
  cloud:
    config:
      uri:
      username:
      password:

---

spring:
  profiles: prod
  application:
    name: gateway-server
  cloud:
    config:
      uri:
      username:
      password: